# aws-cfn-codepipeline-lambda

- codebuild
- codepipeline
- github webhook

## 実行手順

```sh
aws cloudformation deploy --template template.yaml --stack pipeline-lambda-cfn --capabilities CAPABILITY_IAM
```
